export class User {
	_id: string;
	firstName: string;
	lastName: string;
	email: string;
	mobileNumber: string;
	createdAt: string;
	updatedAt: string;
	__v?: number;

	// constructor(public name: string, public state = 'inactive') {

	// }

	// toggleState() {
	// 	this.state = this.state === 'active' ? 'inactive' : 'active';
	// }
}
